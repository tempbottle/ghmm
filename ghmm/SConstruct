import os,syscwd = os.getcwd()run = os.systemlibxml2_path = os.path.join(cwd,'..','..','libxml2-2.9.1')libiconv_path = os.path.join(cwd,'..','..','libiconv')include_dirs = [os.path.join(libxml2_path,'include'),os.path.join(libiconv_path,'libiconv','include')]library_dirs = [os.path.join(libxml2_path,'lib'), os.path.join(libiconv_path,'lib')]libs = ['libiconv','libxml2','kernel32']'''envd = Environment(MSVC_VERSION='9.0',                  CCFLAGS = ['/MD','/DWIN32'],                  CPPPATH=include_dirs)                  envd.SharedLibrary('ghmm',                   Glob('*.c'),                  LIBS = libs,                   LIBPATH = library_dirs)'''envs = Environment(MSVC_VERSION='9.0',                  CCFLAGS = ['/MT','/DWIN32'],                  CPPPATH=include_dirs)                  envs.StaticLibrary('ghmms',                   Glob('*.c'),                  LIBS = libs,                   LIBPATH = library_dirs)